import React from "react";

export class Main extends React.Component<IMainState>{
    render(): React.ReactNode {
        return <div>there will be probably one tab the administration panel will be hidden, for start just
            hardcode some graphs
        </div>;
    }
}

interface IMainState{

}